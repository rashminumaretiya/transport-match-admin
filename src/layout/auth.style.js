import { makeStyles } from '@mui/styles';

export const authStyle = makeStyles({
  authImage: {
    minHeight: '100vh',
    maxHeight: '100vh',
    display: 'flex',
  },
  authWrapper: {
    maxWidth: 465,
    margin: '0 auto',
    minHeight: '100vh',
    padding: 24,
    justifyContent: 'center',
  },
  logoWrapper: {
    maxWidth: 163,
    margin: '0 auto',
  },
});
